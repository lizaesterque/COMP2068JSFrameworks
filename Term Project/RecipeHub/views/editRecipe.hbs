<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Recipe</title>
</head>
    <div class="edit-body">
    <h2> Update Recipe</h2>
    <form action="/recipes/edit/{{ recipe._id }}" method="POST">
        <label for="title">Name:</label><br>
        <input type="text" id="title" name="title" value="{{ recipe.title }}" disabled><br>
        <label for="ingredients">Ingredients:</label><br>
        <textarea id="ingredients" name="ingredients" rows="4">{{#each recipe.ingredients}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}</textarea><br>
        <label for="instructions">Instructions:</label><br>
        <textarea id="instructions" name="instructions" rows="4">{{ recipe.instructions }}</textarea><br>
        <label for="imageUrl">Image URL:</label><br>
        <input type="text" id="imageUrl" name="imageUrl" value="{{ recipe.imageUrl }}" disabled><br><br>
        <button type="submit">Save Changes</button>
    </form>
    </div>
</body>
</html>



